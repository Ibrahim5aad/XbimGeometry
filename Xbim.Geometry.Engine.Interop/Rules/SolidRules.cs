namespace Xbim.Geometry.Engine.Interop.Rules
{
    /// <summary>
    /// IFC Where Rule validators for solid model entities.
    /// Each method enforces a single formal rule from the IFC specification.
    /// </summary>
    internal static class SolidRules
    {
        // Solid WR validators will be added here as the solid factory
        // migration progresses (e.g. IfcExtrudedAreaSolid, IfcRevolvedAreaSolid,
        // IfcSweptDiskSolid, IfcBooleanResult).
    }
}
